/**
 * Return the length of the node.
 *
 * The **length** is the number of the node's children.
 *
 * @param {Node} node Node.
 * @return {Number}
 */
export default function length(node) {
  return node.children.length
}
